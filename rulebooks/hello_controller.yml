---
 - name: Hello Controller 
   hosts: all
   sources:
     - ansible.eda.range:
         limit: 5

   rules:
     - name: "Run job template"
       contition: event.target_os == "linux"
       actions:
         run_job_template:
           name: Git-Playbook
           organization: Default
           retries: 1
           delay: 10
