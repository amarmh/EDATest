---
- name: test
  hosts: all
  sources:
    - ansible.eda.tick:
        delay: 300

  rules:
    - name: test test
      condition: event.i is defined
      action:
        run_job_template:
          name: "EDA - test job"
          organization: Default
    
