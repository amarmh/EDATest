---
- name: test
  hosts: all
  sources:
    - ansible.eda.tick:
        delay: 300

  rules:
    - name: test test
      condition: event.i is defined
        timeout: 2 minutes
      action:
        run_job_template:
          name: "EDA - test job"
          organization: Default
    
